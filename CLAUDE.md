# Afterline — 작업 규칙

이 문서는 Claude가 Afterline 프로젝트에서 따라야 할 절대 원칙과 작업 방식을 정리한다.
전체 명세는 [PROJECT_BRIEF.md](PROJECT_BRIEF.md) 를 참조한다.

---

## 1. 절대 원칙 (이게 무너지면 Afterline이 아님)

- **데이터 구조**: 문장 → 출처 → 수집노트 구조 유지.
- **공간 은유**: 분류 → 문(Door) → 방(Room) → 책장(Shelf) → 책등(Spine) 유지.
- **무드**: 저장앱/다이어리앱 금지. "개인 에디토리얼 아카이브" 느낌.
  - 흑백 기반 + 원색 포인트
  - 세로 타이포그래피
  - 포스터형 / 책등형 레이아웃
  - 영문 라벨이 주, 한글이 보조

---

## 2. 프로젝트 결정사항

- **1차 목표**: 재영 혼자 쓰는 1인용 데모 겸 포트폴리오. 멀티유저 회원가입 UI는 만들지 않는다.
- **Supabase**: 계정은 이미 있음. DB 연결은 뒤쪽 Phase에서 한다.
- **인증**: 나중에 Supabase 매직링크(비밀번호 없는 이메일 로그인) 단일 사용자 방식. RLS(`auth.uid() = user_id`) 정책은 기획안대로 유지.
- **빌드 순서**: 화면·인터랙션(임시 데이터) 먼저 → Supabase 연결 → 책검색/CSV.

---

## 3. 협업 규칙

- 사용자(재영)는 디자이너이고 개발 지식이 깊지 않다.
- 계정 만들기, API 키 발급, 터미널 설정처럼 **사용자가 직접 해야 하는 일**이 생기면, 그 작업을 따로 떼어 단계별로 천천히 안내한다.
- **한 번에 다 만들지 않는다.** PROJECT_BRIEF.md 의 11번 Phase 순서를 기준으로 한 Phase씩 진행한다.
- 각 Phase가 끝나면 멈춰서 다음 세 가지를 사용자에게 묻는다:
  1. 뭘 만들었는지
  2. 브라우저/터미널에서 확인할 방법
  3. 다음 Phase로 갈지
- **큰 결정**(라이브러리 추가, 폴더 구조 변경 등)은 실행 전에 한 줄로 먼저 알린다.
- 코드보다 **"지금 이게 왜 필요한지"** 를 한국어로 짧게 먼저 설명한다.

---

## 4. 코드/스택 기본값

- Next.js App Router + TypeScript + Tailwind CSS.
- 디자인 토큰: `:root` CSS 변수 + Tailwind theme extend 양쪽에 반영.
- 데이터 레이어는 인터페이스로 추상화: 지금은 mock, 나중에 Supabase로 교체. 화면 코드는 데이터 출처를 모르게.
- 폰트:
  - Serif title: Cormorant Garamond / Libre Baskerville
  - UI: Pretendard / Inter
  - Meta: IBM Plex Mono
- 애니메이션은 필요해질 때(주로 Phase 6) Framer Motion 추가.

---

## 5. 금지/주의

- 카툰풍 문 그래픽 금지. 문은 "에디토리얼 패널처럼 동작하는 문".
- 귀여운 다이어리 톤, 일반 노트앱 톤 금지.
- 화면 코드가 Supabase 클라이언트를 직접 부르지 않게 — 항상 데이터 레이어 경유.
